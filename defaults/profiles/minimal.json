{
  "name": "minimal",
  "description": "Minimal — lint+test only, no additional ceremony, fast and focused",
  "includes": {
    "rules": [],
    "skills": ["readme-gen"],
    "agents": [],
    "core_mcp": ["filesystem", "memory"],
    "optional_mcp": ["sqlite", "postgres"]
  },
  "config": {
    "opencode": {
      "compaction": { "auto": true, "prune": false },
      "permission": { "edit": "allow", "bash": "allow" }
    }
  },
  "claude_md": "minimal"
}
