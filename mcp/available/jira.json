{
  "name": "jira",
  "description": "Manage Jira issues, sprints, and boards",
  "server": {
    "command": "npx",
    "args": ["-y", "@anthropic-ai/mcp-server-jira"],
    "env": {
      "JIRA_HOST": "{env:JIRA_HOST}",
      "JIRA_EMAIL": "{env:JIRA_EMAIL}",
      "JIRA_API_TOKEN": "{env:JIRA_API_TOKEN}"
    }
  },
  "requires": {
    "binaries": ["npx"],
    "env_vars": ["JIRA_HOST", "JIRA_EMAIL", "JIRA_API_TOKEN"]
  },
  "setup_notes": "Generate an API token at https://id.atlassian.com/manage-profile/security/api-tokens. JIRA_HOST is your instance URL (e.g. https://company.atlassian.net)."
}
